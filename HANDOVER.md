# HANDOVER: RetireForecast — UK retirement / downsizing forecast tool

> A local-first UK financial-forecasting decision-support tool. A fresh agent picks this up to continue building the calculation engine and then the app around it. Read `docs/PLAN.md` first: it is the full approved plan and the source of truth for scope.

**Stage:** active
**Status:** Phase D go-live, **feature-complete for personal use**; the adviser-legibility workstream and the whole **post-v1 enhancement backlog are built** (annuitisation, historical stress-test, ONS mortality-refresh guardrail, care-cost risk — plus **Lane B forced-housing now complete** (in-place forced sale built 2026-07-03) + Lane C withdrawal-sequencing *core*, #5/#6 handed off). The tool runs in **personal-use advice mode** (`config('compliance.personal_use')` = the flagged regulatory line — set false before any public release). What remains is Rob's **browser verification / sign-off**, the **public-release blockers**, and **optional refinements** — see What's next + Current state.
_Last updated: 2026-07-09 (**app DB moved SQLite → Postgres**, which FIXED the queued-Monte-Carlo reproducibility bug — `queue:work` now reproduces exactly; engine was always sound. Also this session: BTL finance-cost reducer + #17 rental £1,800/mo; bought-home 1% maintenance; #23 child-help tax-free; the £118k base mortgage is a DELIBERATE paydown design — read docs/SCENARIO-V2.local.md before any V2 figure. Session log + DECISIONS.)_

## Multi-agent coordination (lanes CLOSED 2026-07-02 — single-session tree)
The concurrent A/B/C/D lanes are **closed**; this is a single-session tree again. The full per-lane build record
lives in `git log` + DECISIONS.md (2026-06-30 / 07-01) — not restated here. High level: **Lane A** post-v1 backlog
(annuitisation, historical stress-test, ONS mortality-refresh, care-cost stochasticity) — complete; **Lane B**
forced-housing workstream (means-tested benefits, mortgage-redemption + payment-stop, feasibility flags, input
clarity, **in-place forced sale**) — **complete** (the forced sale built 2026-07-03,
[docs/PLAN-in-place-forced-sale.md](docs/PLAN-in-place-forced-sale.md)); **Lane C** withdrawal sequencing — core
shipped, #5/#6 handed off ([docs/PLAN-withdrawal-sequencing.md](docs/PLAN-withdrawal-sequencing.md)); **Lane D**
multi-property — docs-only DRAFT awaiting Rob ([docs/PLAN-multi-property.md](docs/PLAN-multi-property.md); note it was
mis-homed inside commit `5a6688f`, content intact). If lanes ever reopen, honour [[concurrent-session-split]]
(re-check `git` first, commit only your files, never push without Rob's go-ahead). `PathProjector` was the
cross-lane contention point.

## Goal & success criteria
Full plan: [docs/PLAN.md](docs/PLAN.md); PRD: [PRD.md](PRD.md). Summary:

- **Goal:** let an older couple (one working, one retired) model whether to sell their home and either buy somewhere cheaper outright (invest the surplus) or sell and rent (invest all proceeds), and see the consequences of pension lump-sum withdrawals and whether their money lasts for life.
- **Headline outputs:** (1) the pension lump-sum tax shock (25% tax-free, marginal tax on the rest, plus the Month-1 emergency-tax overpayment and reclaim), and (2) running-out-of-money / longevity risk via Monte Carlo.
- **Success for Rob's own use:** a working **local** site where he can enter a real (known) couple himself, run buy-vs-rent, and read a trustworthy forecast. **No hardcoded client data in the repo.** If it proves useful he may later release it publicly for free.
- **Correctness bar:** the engine reproduces known HMRC worked examples to the penny (examples A, B, C in docs/PLAN.md). **Met** for the deterministic engine.

## Canonical data shape
The single source of truth for the domain shape is the engine's readonly DTOs under `packages/finance-engine/src/Dto/` (Eloquent models and Livewire forms map to/from these). See [DATA-MODEL.md](DATA-MODEL.md) and docs/PLAN.md for the full field lists. Conventions, honoured by all existing code:

- **Money = integer pence**, never a float. Held by `Money` value object (GBP only). Rates = `Percent` (integer basis points). Dates = ISO `Y-m-d`. **Ages are derived from DOB + a reference date, never stored.**
- Entities — all coded as readonly DTOs under `src/Dto/` and persisted as encrypted payloads: Household, Person, Pension (subtype dc|db|state), Property, Account (isa|gia|cash|premium_bonds), IncomeStream, ExpenseProfile, Scenario (variant buy_outright|rent|stay_put), AssumptionSet, SimulationRun, Result. Sensitive money/DOB/salary/pot/balance fields are encrypted at rest.
- **Storage inversion (Phase B):** a scenario stores the raw builder **form-state** (`builder_state`, one `encrypted:array`) as the single source of truth; the engine `Household` + `HousingAction` DTOs are **derived** from it (`Scenario::toHousehold()`/`toHousingAction()` via `HouseholdAssembler`, no reverse-mapper). A what-if **child** (Phase C2) holds no `builder_state` — only `parent_scenario_id` + a sparse encrypted `overrides` delta; `effectiveBuilderState()` = base ⊕ overrides via `App\Forecast\BuilderStateDelta`. The delta carries **value overrides, added rows (stored whole at their id path) and removed rows (a `REMOVED` sentinel)**, so a what-if can add/remove items, not only change values (DECISIONS 2026-06-30). Clear columns are a projection. (The pre-rebuild `households`/`scenario_drafts` tables were dropped.)

## Architecture / stack
- **Laravel 13.17** app at the repo root (SQLite locally). **Fortify** (auth) and **Filament 5** admin (which pulled **Livewire 4** — a bump from the plan's stated Livewire 3). Fortify views are on; `FortifyServiceProvider` points each view route at a Blade screen (incl. the 2FA challenge + password-confirmation).
- **Front end:** hand-rolled **Livewire 4** full-page components (`app/Livewire/`) rendering into one Blade layout component via `#[Layout('components.layouts.app')]` — **not** Livewire 4's default `layouts::app` (only a component namespace here, so no view hint path). **ApexCharts** bundled via npm (`resources/js/app.js` + `charts.js`, an Alpine `chart` wrapper, reduced-motion aware, try/catch fallback to the table). `public/build` is gitignored, so the base `TestCase` calls `withoutVite()`.
- **`packages/finance-engine`**: a framework-free Composer **path package** (`retireforecast/finance-engine`, symlinked, required `"*"`). Zero Laravel dependencies, no I/O, no clock. This is the product; the Laravel app is a shell. The engine must never `use App\...` or `Illuminate\...` (guarded by `EngineIsolationTest`).
- Money is **hand-rolled integer pence** (brick/money dropped over a dependency clash). PHPUnit 12. **`phpoffice/phpspreadsheet`** is an **app-layer** dependency (for `.xlsx` import only); the engine stays dependency-free. A CSP + hardening headers ship on the `web` group (`config/security.php`); Filament `/admin` is out of scope by design.

## Key files / structure
The engine is the product; the Laravel app is a shell around it. The full tree is not mirrored here (it would drift) — browse it.

- `packages/finance-engine/src/` — framework-free engine: `Money/`, `TaxYear/` (the per-year config spine), `Tax/`, `Pension/`, `StatePension/`, `Property/`, `Benefits/`, `Iht/`, `Care/`, `Dto/` (canonical shapes), `Assumptions/`, `Benchmark/` (PLSA RLS), `Mortality/` (ONS cohort), `Forecast/` (`PathProjector` + `DeterministicForecaster` + `YearResult`), `MonteCarlo/`, `Housing/`. Namespace `RetireForecast\FinanceEngine\…`; tests are pure PHPUnit (the `Engine` testsuite).
- `app/` — `Forecast/` (`HouseholdAssembler` form-state→DTOs, `BuilderStateDelta`, `ScenarioForecaster`, `SimulationRunner`, `ResultPresenter`, `LumpSumTaxShock`, `AssumptionComparison`), `Import/` (CSV/xlsx `Spreadsheet` + profiles), `Livewire/` (`Dashboard`, `ScenarioBuilder`, `ScenarioResults`, `ScenarioCompare`, `AccountSecurity`), `Compliance/` (`OutputPhrasing` banned-phrase lint + walled-off `Interpretation`), `Finance/Mapping/`, `Models/`, `Http/` (controllers incl. `ScenarioPdfController`; `SecurityHeaders` + `EnsureDisclaimerAcknowledged` middleware), `Filament/`, `Demo/`, `Gdpr/`, `Jobs/`.
- Root: `composer.json` (path repo for the engine), `phpunit.xml` (defines the `Engine` testsuite), `config/security.php` (CSP + toggles), `database/migrations/` (scenarios holds `builder_state` + `parent_scenario_id`/`overrides`; no households/scenario_drafts), `database/seeders/DemoScenarioSeeder.php` (opt-in fictional sample).
- House style is Pint: `vendor/bin/pint --dirty` (app) / `vendor/bin/pint packages/finance-engine` (engine).

## Decisions locked
See [DECISIONS.md](DECISIONS.md) for the full append-only log + rationale. The load-bearing "don't relitigate" anchors:
- **Local-first, personal use, no hardcoded client data.** Rob enters the couple via the UI; any first-run sample must be obviously fictional. Possible free public release later, so do not design accounts out.
- **Modelling depth:** HMRC-accurate deterministic engine PLUS Monte Carlo with **stochastic joint-life mortality**. Pensions DC/DB/State; housing buy-vs-rent on identical seeds; IHT a toggle (incl. pensions entering the estate from April 2027). Assumptions are a sourced, runtime/display choice (FCA default), not baked in.
- **Regulatory posture: education/guidance only** is the **public** stance (never a personal recommendation; `BannedPhrasingTest` partition lint; signpost Pension Wise / MoneyHelper). **Currently relaxed for personal use:** this is a private tool, so **`config('compliance.personal_use')` (default true) is the flagged "regulatory line"** — it turns the walled-off advice-style `interpret` capability ON for everyone (no admin grant), so the app gives direct advice (e.g. the buy-vs-rent "why" narrative). **Set it false before any public release** and the guidance-only partition (lint + per-user `can_interpret` grant) re-applies. **Relaxed further for the private/family phase (2026-07-04):** the suite now runs with it **true** (advice mode), and `BannedPhrasingTest` is **posture-aware** — it *skips* (visibly, with a count) in advice mode instead of blocking direct advice copy, and *fully enforces* the partition when `personal_use=false`. Nothing deleted; the neutral-zone definition has one home (`App\Compliance\NeutralZoneScanner`), and **`php artisan compliance:advice-audit`** lists advice spots on demand (the standing "flag it for later" inventory). Flip the flag false to re-enforce. See DECISIONS 2026-06-30 + 2026-07-04.
- **Engine is framework-free** in a path package; **money = integer pence**; **savings + dividends in one combined income-tax pass**; **tax figures versioned per tax year with source + verified-on** (freeze to April 2031; dividend rates rise in 2026/27).
- **UI = hand-rolled Livewire 4** (Filament admin-only); form input → engine DTOs via a standalone, unit-tested `HouseholdAssembler`; charts are a progressive enhancement (every figure also text + accessible `<table>` + CSV); the region guard asks the engine's `TaxYearRegistry` (Scotland refused until its bands land).

## Current state
- **Done — deterministic engine:** money layer; per-year `TaxYearConfig`/`TaxYearRegistry` (2025-26 + 2026-27, England/Wales/NI; Scotland throws); income tax (combined savings/dividend stacking) + NI; pension lump-sum suite (PCLS/UFPLS/drawdown, Month-1 emergency tax + P55/P50Z/P53Z, MPAA, AA + taper) — **worked examples A & B**; State Pension (SPA-from-DOB, deferral, triple lock); SDLT (+surcharge) + CGT (PRR); benefits capital tariff + £16k cliff — **worked example C**; IHT (pensions-in-estate toggle) + care means-test. Plus DTOs, `AssumptionSetLibrary` (3 sourced sets), ONS cohort mortality, `Forecast/` (`PathProjector` + deterministic + Monte Carlo), `Housing` buy-vs-rent on identical seeds. **A5** (GIA/cash income tax + CGT-on-disposal) complete.
- **Done — app layer:** encrypted DTO persistence + Fortify auth + GDPR export/erase + Filament admin; forecast/scenario services (`ScenarioForecaster`, `SimulationRunner` + queued `RunScenarioSimulation` with progress + cancel); Livewire UI + ApexCharts (auth screens, builder wizard, results page, Compare); compliance layer (partition lint, first-run disclaimer gate, walled-off interpretation toggle); spreadsheet import (CSV/`.xlsx`, calibrated profiles); lump-sum tax-shock panel; compare-assumptions overlay.
- **Done — rebuild:** Phase A (engine enrichments: ongoing contributions, longevity, usable-vs-total wealth, income-by-source), C3 (results usable-vs-total + cashflow ladder), B (`builder_state` storage inversion + edit-in-place + stale-run invalidation), C2 (delta-child what-ifs + Compare), C1 (3-tier line-item budget, core + fast-follow), C4 (PLSA Retirement Living Standards benchmark).
- **Done — Phase D Tier-1 (trust), COMPLETE:** A5; the gov.uk ⚠️ figure-verification pass (every statutory figure re-confirmed + stamped `verified_on: 2026-06-27`, no value changed, pensions-in-IHT now enacted); admin-panel lockdown (`is_admin`); forecast-boundary reconciliation invariants; displayed-figure provenance; the user-facing import reconciliation panel.
- **Done — Phase D Tier-2 (go-live polish), BUILD COMPLETE:** demo preset/seeder; 10k-path Monte Carlo perf (lean `IncomeTaxCalculator::totalPence()` + worker JIT); CSP + security headers; 2FA enrolment UI; PDF results export (dompdf, reuses `ResultPresenter`); a11y CI scaffold + a first local sweep (3 contrast fixes); queued-run "waiting for a worker" hint (`SimulationRun::isAwaitingWorker()` → neutral on-screen note when a run sits queued at 0% past a 15s grace window).
- **Done — adviser-legibility *presentation* layer (2026-06-29, pending Rob's browser sign-off):** on the results page — the **house-sale waterfall** (`ResultPresenter::saleExplainer` + a new reconciled engine value object `HousingPurchase`), the **assumptions panel** (`assumptionsPanel`, real-vs-nominal labelled, engine's own blended return), **itemised per-year spend** (essential/discretionary in the cashflow ladder), **life-event milestones** (`milestones` + a new single-source engine field `ForecastResult::deathCalendarYears`), and **input-sanity notes** (`inputNotes`). Each carries a reconciliation/labelling guard; all education/guidance-only (the banned-phrasing lint).
- **Done — #1 contingent-cost correctness fix (2026-06-29, option b):** an expense line carries an auto-classified **condition** (mortgage / service charge → *while owning the home*; commute → *while working*; explicit override honoured first). `ExpenseProfile` gains `propertyCosts` / `employmentCosts` markers (a subset of essential, no drift); the **sell variants build with `withoutPropertyCosts()`** (no phantom mortgage on a sold home) and **`PathProjector` drops the commute when no one earns** (it stops at retirement, every variant); `HousingComparison::variantInputs()` is the new single source of the three variant households (the Monte Carlo runs it; the #6 ladder will too); PLSA now excludes property costs on its outright-ownership basis. Reconciliation-tested (property costs only in stay-put; commute falls by its full amount at retirement; auto-classify + override + PLSA-exclusion). **The per-line override UI is still to build** (auto-classification gives the defaults today). The **everything-editable / buy-vs-rent direction** is still to build — see What's next.
- **Done — #6 per-variant deterministic cashflow ladder (2026-06-29):** `ScenarioForecaster::deterministicVariants()` runs each housing strategy through `DeterministicForecaster` on the variant household + settings from `HousingComparison::variantInputs()` (the single source the Monte Carlo comparison also runs, so they can't drift; `stay_put` == the old `deterministic()`). The results page gained a **strategy selector** (`ScenarioResults::$ladderVariant`, default = the scenario's own variant) driving the cashflow ladder + its milestones; the **house-sale milestone** now lands (year 0, household-level, no per-person age) for a sell strategy; the **PDF** ladder follows the scenario variant too. Provenance (panel == CSV == PDF) holds on the *selected* variant; income-floor / input-sanity notes deliberately stay on the raw (stay-put) projection. Reconciliation-tested (sell variant owns no home → usable == total, equity freed into liquid wealth; buy keeps a smaller home).
- **Done — the builder highlights a what-if's changed inputs + shows the base value (2026-06-30):** when editing a what-if, each input whose value differs from the base is **ringed in amber and shows the base value it diverged from** ("was £18,000"), plus a one-line "fields you change from the base are highlighted" banner. `ScenarioBuilder::changedFromBase()` computes a `path => formatted base value` map (a **positional** diff of the live `builderState()` vs the base's `effectiveBuilderState()`, **index-based** to match each input's `wire:model`; values formatted by the shared `WhatIfChanges::formatValue()`), renders it on the form (`data-builder-diff` + a `data-changes` object), and a bundled, CSP-safe, morph-aware `resources/js/builder-diff.js` rings each matching input (`.builder-diff-changed`) and shows its base value via the field wrapper's `::after` (`.builder-diff-field[data-original]` — **not an injected node**, so morph-safe). One server map + one script covers all ~70 inputs uniformly. Pure progressive enhancement. Livewire-tested (childMode maps the changed path to its base value + renders the hook; a base does neither). **Pending Rob's browser sign-off.**
- **Done — one-click "quick what-ifs" (2026-06-29):** preset buttons **"Retire 2 years later"** and **"Live 10 years longer"** on the base's results page and each dashboard base row. Each POSTs to `QuickWhatIfController`, which uses `App\Forecast\QuickWhatIf` to edit the base's people (retire-later bumps each *working* person's `plannedRetirementAge` +2, clamped 50–80; live-longer moves each person onto a +10-year `offset_years` longevity lever relative to the base) and stores the result as an **ordinary delta-child via `BuilderStateDelta::diff`** (minimal, structurally identical — so it highlights/compares/edits like any hand-built what-if), then opens it. A preset that would change nothing builds nothing (no empty what-if); repeats get distinct names; owner-scoped. First UI use of the per-person longevity lever. Service + endpoint tested. **Pending Rob's browser sign-off.**
- **Done — what-ifs highlight what they changed from the base (2026-06-29):** a delta-child what-if now surfaces its `overrides` as readable changes in three places — a **"What this what-if changes" panel** at the top of its results page (each change as **base → new**, plus a "what-if of <base>" header line + orphaned-override notice), compact **change tags** on each what-if row in the **dashboard**, and per-plan **change chips** in **Compare**. One presenter `App\Forecast\WhatIfChanges` turns the sparse override map into `{label, from, to}`: the base value an override replaced is read back via the new **`BuilderStateDelta::valueAt()`** (read mirror of `setPath`, row-id aware), each dot-path humanised (list rows named by their own label/identity, e.g. "Essentials · amount", "DC pension · current value"), money/rate/enum formatted, meta fields (name/step) excluded. A pure projection of the existing delta (no new store). Unit + Livewire tested. **Pending Rob's browser sign-off.**
- **Done — editable-assumptions layer, *core* (2026-06-29):** the six economic assumptions surfaced by the read-only panel (investment growth blended-real, CPI, house/rent/salary growth, income yield) are now **editable on builder step 1**, defaulting to the chosen preset (placeholder + named in the hint) and deriving a **custom set**. Stored as a **sparse `assumptionOverrides` delta** in `builder_state` (only changed figures; empty keeps the preset, so a re-source flows through; composes with delta-child what-ifs via `BuilderStateDelta`). Engine `AssumptionSet::with*` (immutable; `withRealReturnShift` lands the blended-real return on the target without diverging the Monte Carlo) + `App\Forecast\AssumptionOverrides::apply()`, applied in the **single** resolution point `ScenarioForecaster::assumptions()` (so deterministic + variants ladder + Monte Carlo + the frozen run snapshot all share it). Results panel labels a tuned set **(customised)** and marks each **user-set** figure. Reconciliation-tested (no overrides == the preset; an edit demonstrably reaches the forecast; the blend lands on target under any allocation). **Pending Rob's browser sign-off.**
- **Done — results-page "on this page" side nav (2026-06-29, browser-verified desktop):** a sticky 2-col grid nav on `lg+` (hidden on mobile), listing only the sections present this render (built from the same flags they render under) as real anchor links that work without JS, with a CSP-safe `IntersectionObserver` scroll-spy (`resources/js/toc.js`). **Mobile check deferred** by Rob to later in the dev timeline.
- **Done — editable-assumptions layer UI (2026-06-30, pending Rob's browser sign-off):** (a) a sticky **live in-builder preview** running one cheap deterministic forecast on a transient scenario from the current form-state (never saved), recomputed each round-trip, headlining the does-the-money-last **verdict** + **spendable/total wealth at end**, inviting completion while the inputs are too incomplete to forecast (single-sourced from `ScenarioForecaster::deterministic()`, server-rendered, CSP-safe); (b) the **modelled age at death** shown beside each person's lifespan lever (from `ForecastResult::deathCalendarYears`, same forecast), so "peer/+10 years" resolves to a visible age; (c) **selling costs decomposed into per-component %/£ lines** — engine `SellingCostComponent` (`Percent|Money`) summed in `saleProceeds` with a reconciled `HousingProceeds` breakdown; default 2% preserved when none; legacy `sellingCostRate` back-compat (total preserved); builder edits three default components (agent 1.25%, legal £1,500, EPC & removals £800) each with a %/£ toggle; (d) the **per-line cost-condition override** ("Applies": Auto / Always / while-owning / while-working) completing option (b), with an Auto hint from `HouseholdAssembler::autoCondition()`; (e) the **per-line include/exclude toggle** — an "Include this cost" checkbox switches a line off (kept but counts £0, row dimmed, live preview moves); the assembler drops excluded lines once in `household()` so every total excludes them; the flag is stored sparsely (only when off) so it records no spurious what-if delta.
- **Done — buy-vs-rent compare + personal-use advice mode (2026-06-30):** a one-click **"Compare buy vs rent"** button (`BuyVsRentCompare` + `BuyVsRentController`) generates the meaningful alternative strategies as variant-only delta-child what-ifs and opens **Compare**, which now projects **each plan on its own variant** (`deterministicVariants[plan.variant]`). The regulatory line is flagged at `config('compliance.personal_use')` (default true): in this mode the `interpret` Gate is on for everyone and the walled-off `Interpretation` layer gives direct advice — the buy-vs-rent **"why" narrative** (`Interpretation::compareNarrative`, ranks the plans). The suite runs with the flag **false** (public posture) so the guidance-only partition stays tested. See DECISIONS 2026-06-30.
- **Done — what-ifs can add/remove items (2026-06-30):** the delta represents structural changes — an **added** row stored whole at its id path, a **removed** row as a `BuilderStateDelta::REMOVED` sentinel; `merge`/`setPath` append adds and splice removals, while a *leaf* override to a base-deleted row stays a flagged orphan. The "a what-if only changes values" refusal is gone; `WhatIfChanges` shows an add/remove as one line; the builder highlight pairs base rows by id. See DECISIONS 2026-06-30.
- **Done — partial-PRR CGT on selling a let former home (2026-06-30):** occupation-driven (not mortgage type, gov.uk HS283) — a `CgtHistory` on the engine `Property` (null = full PRR / £0, the common case), `CgtPrivateResidenceCalculator` extended for **joint owners** (two allowances), wired into `HousingComparison::saleProceeds` (gain = sale − purchase − improvements − selling costs). A **"Capital gains on sale" wizard** under the home (revealed when ever-let) captures purchase price / year / costs / joint + higher-rate toggles / a lived-in-vs-let **period timeline**, with a live readout; the sale waterfall shows the working. See DECISIONS 2026-06-30.
- **Done — longevity distribution (2026-06-30, first post-v1 backlog item):** the Monte Carlo now surfaces a `LongevityDistribution` on `SimulationResult` (read off the same joint-life sampler the wealth paths run) — last-survivor age p10/p50/p90, the planning horizon in years (p50 + p90), and P(at least one of you reaches 95 / 100). Shown as a neutral **"How long the money may need to last"** results-page panel (+ on-this-page nav). Nullable + mapper back-compat (old runs rehydrate as null). Engine + mapper tested. See DECISIONS 2026-06-30. **Pending Rob's browser sign-off (needs a completed run).**
- **Done — source-freshness guardrail (2026-06-30):** a `figures:freshness` command (pure, unit-tested `App\Finance\FigureFreshness`) reports each supported tax year's gov.uk `verifiedOn` and flags any older than `--months` (default 12), exiting non-zero so CI/a periodic run catches aging statutory figures. `TaxYearRegistry::SUPPORTED_TAX_YEARS` is the single source of the year set. A command (not a date-dependent phpunit test). See DECISIONS 2026-06-30.
- **Done — per-year surplus/shortfall + configurable safety floor (2026-06-30):** the cashflow ladder classifies each year **surplus / drawing / shortfall** on usable money and flags years usable funds fall below a **safety buffer** (default 2 months of essentials, set in the Spending step, read via `Scenario::safetyBufferMonths()` → `ResultPresenter::ladder`), with a headline (stays above / dips below in YYYY / runs out in YYYY) and status-tinted rows. Replaces the academic "neutral diagnostics" idea (Rob's reframe). See DECISIONS 2026-06-30.
- **Done — what-if sliders + retirement-year proration (2026-06-30):** an **"Explore the levers"** results-page panel (live sliders: retire ± yrs / spend ± % / return ± pts / live ± yrs) re-runs a throwaway deterministic forecast and shows the outcome — exploratory, never saved (`ScenarioResults::sliderForecast`/`applySliders`, transient scenario). And the engine now **prorates salary in the retirement year** (birth-month ÷ 12) instead of dropping the whole year (`PathProjector::workFraction`). See DECISIONS 2026-06-30.
- **Done — annuitisation (2026-07-01, Lane A):** a DC pot can buy a **lifetime annuity** with part of its value at a chosen age — the pot falls by the amount and pays **amount × rate** for life. `AnnuityPurchase` DTO on `DcPension` (level or RPI/CPI-escalating, single- or joint-life with a survivor %); `PathProjector` buys it once and pays escalating, survivor-aware income mapped to the existing `other_taxable` source (no `INCOME_SOURCES` change). The **rate is a user input** (builder default a sourced ~7.2%), so no fabricated age/rate table is baked in. Builder toggle "Buy an annuity with part of this pot", stored sparsely (no spurious what-if delta). Engine + assembler + builder round-trip tested. See DECISIONS 2026-07-01. **Pending Rob's browser sign-off.**
- **Done — historical stress test (2026-07-01, Lane A):** a **sequence-of-returns backtest** — replay each past year's real UK returns + inflation over the current plan (`HistoricalSequenceDraws`, a third `PathDraws`; `HistoricalBacktester` runs every eligible start year). A results-page panel (deterministic, pre-run) shows the % of ~140 historical starts survived, the worst start, and named crises (1929 / 1973–74 / 2000 / 2007–08). Data = the sourced `HistoricalReturns` class (UK real equity/bond/cash total returns + inflation 1871–2020) from the **Jordà–Schularick–Taylor Macrohistory** database — **CC BY-NC-SA, flagged in-code as a public-release blocker** (swap the source before any public launch). `RepresentativeDeathAge` extracted so forecast + backtest share the median-lifespan rule. See DECISIONS 2026-07-01 + docs/RESEARCH-stress-test-and-official-sources.md. **Pending Rob's browser sign-off.**
- **Done — ONS mortality refresh + integrity guardrail (2026-07-01, Lane A):** a **`mortality:refresh`** command (fully ONS) that (1) verifies the generated `OnsPeriodMortalityData` still matches its JSON source home cell-for-cell (5100 cells — closes the gap that nothing checked the hand-generated class against its source), (2) flags the ONS data stale past `--months` (default 24), and (3) with `--against <newRelease.json>` diffs a fresh ONS release and shows the cohort-life-expectancy impact. Non-zero exit on drift/staleness (CI signal). Pure grid work in `App\Finance\MortalityDataset` (unit-tested incl. the in-sync guard); reuses `FigureFreshness`. Ingests the ONS data as the JSON resource shape (xlsx auto-parse deferred). See DECISIONS 2026-07-01.
- **Done — care-cost stochasticity (2026-07-01, Lane A — last backlog item):** the Monte Carlo can model the fat-tail risk of late-life residential/nursing care. `CareCostSampler` draws a per-person end-of-life spell (Bernoulli ~1-in-4; exponential ~2.5yr duration; residential/nursing self-funder fee) from sourced `CareAssumptions` (Dilnot/PSSRU/LaingBuisson); wired via `PathDraws::careAnnualCost` (deterministic + historical return 0) → charged as essential spend in `PathProjector` → accumulated on `ForecastResult::careCostReal`; `Simulator` aggregates a `CareImpact` (share of paths + median/p90 bill) onto `SimulationResult`. **Opt-in** (`ForecastSettings::modelCareCost`, a sparse builder toggle — existing runs unchanged); the rent variant propagates it. Surfaced as a results **care-risk panel** (visible, not buried). v1 charges the gross self-funder cost (means-test refinement flagged). See DECISIONS 2026-07-01. **Pending Rob's browser sign-off.**
- **Done — in-place forced sale (2026-07-03, last Lane-B item):** a `MortgageMaturityAction::ForcedSale` home is now sold **in place at the redemption year** (an additive `PathProjector::projectYear` event), instead of the old no-op that kept the home for ever. It sells at the grown whole-property value via the shared **`HousingProceeds::compute`** (extracted so the year-0 variants and the in-projection sale are ONE reconciled definition), frees the net proceeds into the first living person's GIA (basis = proceeds), clears the home + debt (new state `homeSold` + `propertyWhole`), and from that year stops the mortgage payment + property/running costs and charges the entered rent. The freed equity is liquid, so Pension Credit erosion falls out for free. Rent + selling-cost basis ride on `ForecastSettings` (`annualRent`/`rentInflationReal`/new `sellingCosts`), populated by `ScenarioForecaster::settings()` **for a ForcedSale scenario only**. `ForcedSaleTest` pins wealth conserved across the sale to the penny + costs-stop-rent-starts + Pension-Credit erosion + let-vs-lived-in CGT; a `ScenarioForecasterTest` completeness test pins the rent/costs reaching the settings. v1 limits (flagged): partial-`ownershipShare` sale reconciles penny-exact only for whole ownership; Pension Credit sees the freed capital from the year after the sale. See DECISIONS 2026-07-03. **Browser-verified (works); the follow-up below made the mortgage-maturity treatment a user input.**
- **Done — mortgage-maturity is a user-modelable input (2026-07-03):** `mortgageMaturityAction` + `mortgageRedemptionYear` are now **editable in the builder** (property step) so refinance / repay-from-capital / forced-sale are each user-buildable scenarios (they were engine-consumed `Property` fields with no UI). The one-click **"Stay put"** plan (`BuyVsRentCompare`) now resets a `forced_sale` to `refinance` so it genuinely keeps the home instead of silently selling. **Migration:** a `forced_sale` base's *existing* stay-put child still inherits the forced sale — regenerate it. See DECISIONS 2026-07-03.
- **Done — local-model assistant, Phase 1 (grounded scenario-explainer) (2026-07-03):** an in-page "chatbot" (`App\Livewire\ScenarioAssistant` on the results page) that explains **this** forecast in plain English on a **local** model (Ollama). App-layer only, engine untouched. `App\Assistant\`: `OllamaChatClient` behind a `ChatClient` interface (local HTTP, fails loudly via `AssistantUnavailable`, strips `<think>`); `ScenarioContext` — the engine figure snapshot that is BOTH the model's context and the grounding allow-list, carrying the headline facts **plus the full year-by-year cashflow ladder** (per-year essentials / discretionary / income-by-source / tax / wealth, from the reconciled `ResultPresenter::ladder()` rows, inline-labelled), the **Monte Carlo probabilities/ranges** (chance the money lasts / runs out, wealth spread, longevity, care — when a completed run exists; else it says so), and the **lump-sum tax shock** (the flagship: 25% tax-free, marginal tax, Month-1 emergency over-deduction + reclaim — when planned), and the **home-sale waterfall** (what you pocket after mortgage / costs / CGT, then invest-and-rent or buy-cheaper — for a sell strategy), so the reader can **interrogate data the UI doesn't spell out**; `FigureGrounding` (**G1** — refuses any £/year/% figure not engine-derived), reuses `App\Compliance\OutputPhrasing` (**G2** — recommendation phrasing blocked in guidance-only mode, allowed in advice mode via the `interpret` gate); `AssistantService` orchestrates with one corrective retry then refuses rather than show a bad answer. **Inert by default** (`config('assistant.enabled')`, env `ASSISTANT_ENABLED`). 19 unit tests incl. the "never surfaces an invented figure" invariant; **end-to-end verified against the real `qwen3:14b`** (grounded answers pass; it declines to invent). v1 = synchronous call with a "Thinking…" state (superseded 2026-07-08: the generation is now a queued worker turn — see the 2026-07-08 Done bullet); it's a **docked full-height side panel** on the right edge (an edge-tab when closed, not a floating chat bubble), with a **Clear** (wipes the local transcript) and **grouped, clickable starter questions** — "Your plan" / "Risks worth checking (Pension Wise / adviser)" = the COBS 9.4.10G drawdown risk warnings in plain English / "Tax and the home" (sale starter shown only for a sell strategy) / "How the forecast is worked out" (Phase-2 methodology). streaming / pre-computed aggregates (the model won't sum across years — G1 refuses an ungrounded aggregate) / a side-nav entry are fast-follows. See DECISIONS 2026-07-03 + docs/RESEARCH-local-assistant.md. **Pending Rob's browser sign-off** (enable the flag, open a results page).
- **Done — local-model assistant, Phase 2 (methodology doc-RAG) (2026-07-03):** the "how does it model X?" questions, on local embeddings (`nomic-embed-text`). App-layer only. New `App\Assistant\`: `EmbeddingClient` + `OllamaEmbeddingClient` (local-only, fails loudly); `DocChunk`/`DocChunker` (heading-anchored, hard-word-capped so no chunk exceeds the embed model's context); `DocIndex` (**hand-rolled cosine search, no vector DB**); `MethodologyRetriever`. An **`assistant:index-docs`** command builds a **gitignored** JSON index under `storage/app/private/assistant/` (a derived artifact; a **missing index degrades gracefully** — methodology just isn't attached, scenario answers unaffected). **Not intent-routed:** each turn embeds the question, cosine-searches, and prompt-stuffs the top chunks above a threshold under a labelled METHODOLOGY section (the Livewire component does the I/O and threads it into `AssistantService::answer()`; the service stays pure/unit-testable). G1 grounding widens to context+methodology (a real methodology figure is groundable, an invented one still refused); the prompt forbids quoting a methodology figure as the reader's own (LA-6/LA-8). **The corpus is CURATED — the load-bearing call (LA-9), forced by live verification:** indexing all of `docs/` surfaced internal planning/build-record prose as "methodology" and nomic's compressed cosines gave no discrimination, so the index is limited to the sourced-methodology docs (`config('assistant.methodology_docs')` = ASSUMPTIONS.md, MORTALITY.md, RESEARCH-stress-test-and-official-sources.md); methodology questions now score 0.70–0.76 (right source doc), scenario questions 0.58–0.63 (nothing attaches at 0.66). Broader engine-computation methodology (emergency tax, Monte Carlo) is now covered by **docs/METHODOLOGY.md** (see next bullet). 15 new unit tests; **verified end-to-end vs real `qwen3:14b` + `nomic-embed-text`** (a mortality-methodology question answers grounded + sourced; a scenario question attaches nothing). **Phase 3 (backlog capture — the model's only write) remains specced.** See DECISIONS 2026-07-03. **Pending Rob's browser sign-off** (build the index, enable the flag, open a results page).
- **Done — /methodology page + doc (2026-07-03):** a purpose-written **`docs/METHODOLOGY.md`** covering the engine's computations (income tax stacking, the lump-sum tax shock + emergency tax + reclaim forms, MPAA/AA, State Pension, SDLT/CGT/PRR, means-tested benefits, IHT, care, mortality, the Monte Carlo, the year-by-year projection) plus a thorough **"what we don't model"** list. **One source, two homes:** it is BOTH the public **`/methodology`** page (`MethodologyController` + `Str::markdown`, mtime-cached, scoped `.methodology-prose` CSS, footer-linked) AND part of the assistant's methodology corpus. **Accuracy-first:** written from a **code-grounded survey of the actual engine** (not planning prose), with the honest caveats (IHT + care means-test are standalone, not in the year loop; SDLT surcharge + Pension-Credit carer addition exist but aren't wired live; the lump-sum panel omits SP/DB other income). Corpus re-indexed (43 chunks); **verified E2E vs real `qwen3:14b`** — emergency-tax / Monte-Carlo / let-home-CGT questions now answer accurately + grounded (before: planning noise or nothing). See DECISIONS 2026-07-03. **Pending Rob's browser sign-off** (open `/methodology`).
- **Done — local-model assistant, Phase 3 (idea capture — the model's only write) (2026-07-03):** the panel's **"Ideas" tab** captures a reader's "we should look at X" idea to a work queue. `App\Assistant\BacklogCapture` structures free text into {kind (research|feature|task), title, note} via the local model, appended to an **append-only, attributed, reversible** `assistant_backlog_items` table (NOT a curated doc; unencrypted — tool ideas, not finances; cascades on user delete). Safe by construction: an unreachable/unusable model falls back to saving the raw idea as a Task (never lost). Review list with delete; on-screen confirmation. **The model never builds** — promotion into docs/PLAN.md stays a human/Claude Code act (an **`assistant:backlog`** command lists the queue). 9 new tests; **verified E2E vs real `qwen3:14b`** (equity-release→feature, Scottish-bands→research, reclaim-wording→task). **This completes the assistant's three specced phases.** See DECISIONS 2026-07-03. **Pending Rob's browser sign-off** (enable the flag, open the Ideas tab).
- **Done — wealth-over-time charts continue below £0 (funding gap) (2026-07-04, from Rob's browser review):** the charts floored at £0 because the engine floors wealth there (assets can't go negative; running out = £0 wealth + a separate `unmetSpend`). Added `SimulationResult::netPositionFanChart` = usable − Σ unmet spend (one continuous line dipping negative to show the *cumulative shortfall*, on the full-target basis Rob chose), computed per-path in `Simulator`, mapped with back-compat (old runs fall back to the £0-floored `usableFanChart`). `ResultPresenter` fan + Compare **burndown** now plot net position (their data tables too), dropping the `yaxis.min = 0` only when a series goes negative; `charts.js` `gbpAxis` is now sign-aware (`-£80k`). Reconciliation held: the burndown = ladder usable − Σ unmet (equal while solvent). The cashflow-ladder *table* deliberately still shows usable wealth ≥ £0 + its separate `shortfall` column (you don't hold −£80k of cash). **Pending Rob's browser sign-off** (the MC fan needs a re-run — old runs lack the field).
- **Done — Compare page shows live Monte-Carlo progress (2026-07-04, from Rob's browser review):** "Re-run all N" queued the runs then showed a one-shot static note — the batch ran silently (violating "no silent long-runs"). `ScenarioCompare` now tracks the batch run IDs and polls (1.5s) an owner-scoped progress panel until every run is terminal: an aggregate bar + "X of N done", per-plan status/% bars, **Cancel all**, and the same **awaiting-worker** hint the results page uses; the run button disables while active; a family run already in flight is **restored on mount**. Reads the existing `SimulationRun` status/`progress_pct` (no data-model change).
- **Done — NI category is derive-default-plus-override (2026-07-04, from Rob's browser review):** the per-person National Insurance field now shows **only when the person is `Employed`** (employment made live), defaults to **Standard**, offers just **Standard / Reduced rate (married woman's/widow's) / Deferred (second job)** in plain English, and **drops the manual "over State Pension age" and "not liable" options** — both are auto (NI stops at SPA and only applies to a salary; the engine already returns £0 in those cases). Behaviour-preserving. See DECISIONS 2026-07-04.
- **Done — IHT wired into the forecast, relationship-status aware (2026-07-04):** the previously collected-but-unconsumed `ihtModelled` toggle now **bites** — `ForecastSettings::modelIht` drives `PathProjector` to value the estate at each death (via the new pure `Iht\EstateValuer`) and compute the IHT due, surfaced on `ForecastResult::iht` (an `Iht\IhtOutcome`). A new `Household::relationshipStatus` (married/civil-partner vs cohabiting, default married) drives the first-death spousal exemption + the final-death transferable band (×2); a `homeToDescendants` builder toggle (default on) unlocks the residence band. Computed nominal-at-death against frozen bands (real fiscal drag), deflated to real; pensions in the estate from April 2027. Results **IHT panel** (education-only + signpost) + **Compare** column + **PDF** section, all deterministic (shows pre-run). Cohabiting **survivor caveats** (DB survivor pension may not apply; State Pension not inheritable) surfaced as input-sanity notes. Built in six green slices; completeness-tested (`InheritanceTaxForecastTest`: the toggle bites; relationship status changes it). A **Monte-Carlo IHT distribution** followed (build-order #7): each MC path already computes its own IHT, so the Simulator aggregates an `IhtDistribution` (share of futures leaving any IHT, median + p90; nullable + mapper back-compat) shown as a "how this varies across your futures" block inside the IHT section once a run has modelled it. See DECISIONS 2026-07-04 + docs/PLAN-iht-and-relationship-status.md. **Pending Rob's browser sign-off** (build a scenario with the IHT toggle on; open its results page — the deterministic panel shows pre-run, the distribution after a run). Backlog (flagged): lifetime gifts / trusts / reliefs; non-descendant beneficiaries.
- **Done — "later-life care isn't modelled" heads-up (2026-07-04):** with the care toggle off (the default), a ~1-in-4 six-figure risk was left off the results page with **no mention** — a silent omission (Delta-research backlog). Now an amber note (shown regardless of run state, suppressed once a run models care) names the risk + points at the builder toggle. Education-only (passes the guidance-only lint in enforce mode); Livewire-tested.
- **Done — two Blade `word@if` gluing bugs fixed + guarded (2026-07-04):** a control directive glued to a word char (`finished@if`, `payments@if`) silently fails to compile and leaks literal `@if`/`@endif` to the page (and renders the conditional body unconditionally). Fixed the new progress panel (`@elseif`) and a **pre-existing** PLSA-footnote instance on the results page (ternary echo). Now guarded app-wide by `BladeDirectivesCompileTest` (compiles every view, fails on any leaked directive); Livewire panel tests gained `assertDontSee('@endif')`. See [[blade-directive-word-glue-gotcha]].
- **Done — decision-support Phase 1: the queued, persisted threshold backend (2026-07-05):** the compute core (`LeverThresholdService`) is now a first-class run. `App\DecisionSupport\ThresholdRunner` mirrors `SimulationRunner` (createRun / **request-or-cache-hit** / execute-with-progress-and-cancel); the `RunLeverThreshold` job mirrors `RunScenarioSimulation`. A single `ThresholdResult` model + migration is BOTH the run (lifecycle + live progress + cancel, reusing `SimulationStatus`) AND the store (the mapped `ThresholdOutcome` = curve + crossing in the encrypted `payload`, null until done; `ThresholdOutcomeMapper` — float lever-space, not pence). **Two-layer staleness:** `ScenarioBuilder` deletes `thresholdResults()` on edit exactly as it deletes runs (cascading to children), *and* each record is keyed by an **inputs hash** (sha256 of the effective builder-state + engine version + lever/metric/target/grid/paths/seed) so a re-request is a cache hit and a stale figure never surfaces. Provenance frozen per record (seed/paths/grid/engine + tax-year versions/assumption snapshot). An owner-scoped **CSV** route + `ThresholdCsvExporter` carries the shared `App\Export\ExportDisclaimer` (**extracted from `ScenarioResults`** — one home for the guidance wording), the crossing verdict (a band, S3) + the full grid. Default 2,000 paths/point (`ThresholdRunner::DEFAULT_PATHS`). Additive migration (a new table, no data change) — no re-run needed. See DECISIONS 2026-07-05 + docs/PLAN-decision-support.md. **Backend only — no UI yet (Phase 2); nothing links to the CSV route yet.**
- **Done — decision-support Phase 2: the "How far can we go?" results panel (2026-07-05):** a nested Livewire component `App\Livewire\ThresholdExplorer` (`sec-how-far`, embedded on the results page like the assistant) for the non-numbers decision-maker. Pick one lever the scenario can move (buy price only when a buy is configured, retirement age only when someone still works, essential spend always), **drag it** and an **instant deterministic net-position line** redraws (new transient entry point `LeverThresholdService::deterministicForecastAt` — applies the lever via the same `SweepLever::apply` the sweep uses, runs one `DeterministicForecaster`, no MC/persistence; the line is explicitly labelled "a central estimate, not the range" so it's never mistaken for the probability answer). **"Find the limit"** runs the queued Phase-1 threshold and paints a **green→red meter** (boundary = the MC crossing, on-track side from the lever's monotone direction). A **10-dot natural-frequency pictograph** headlines the current plan's MC odds (year-first, from `latestCompletedRun`). An analyst **"full sweep" disclosure** holds the S-curve + grid + the Phase-1 CSV route. `App\DecisionSupport\ThresholdPresenter` builds the view models, **reusing `ResultPresenter::burndown`** so the line shares the one usable-wealth definition (liquid+pension) with the ladder/Compare. **Guardrails enforced + tested:** no "safe" in neutral copy; dots+year not a bare-% headline; icon+text chips (never colour-alone); death vertical recoloured off the shortfall-red band. See DECISIONS 2026-07-05 + docs/PLAN-decision-support.md. **Deferred fast-follows:** start the slider at the scenario's *actual* value (currently the grid mid-point); a simple-mode MC fan (median+floor) as an alternative to the deterministic line. **Pending Rob's browser sign-off** (open a completed scenario's results, scroll to "How far can we go?", drag a lever, click "Find the limit" — needs the queue worker running).
- **Done — decision-support Phase 3: the combination-comparison surface (2026-07-05):** a new section on the **Compare** page ("Chance the money lasts, across your futures") that scores each compared plan (base + ready children) on its **latest completed Monte Carlo run** — its own surface, deliberately kept apart from the deterministic Yes/No table. Each plan shows a plain **word-band chip** (`ResultPresenter::lastsBand()` — strong→poor, **no decimals** because 94.9 vs 95.0 is MC noise, never the word "safe") over a **net-position sparkline** (the same usable − Σ-unmet series as the fan/burndown, so a glance can't drift from the ladder), its what-if change tags, and a per-plan **drill-down** (chance essentials/full-spend last, runs-short %/year, p10 + median usable wealth, paths) with an owner-scoped, base-centric **CSV** (`CombinationCsvExporter` + `CombinationCsvController` at `scenarios.compare.csv`, carrying the shared `ExportDisclaimer`). **Ordering is advice, so it is gated:** the neutral `App\DecisionSupport\CombinationComparison` presenter is **unordered** (plan order, base first) and never labels one plan "strongest"; best-first reordering + the "which to lean towards" narrative come from `Interpretation::combinationRanking()` (the walled-off layer) **only behind the `interpret` ability** — the row sort uses the same comparator the narrative ranks by, so they can't disagree, and unsimulated plans sink to the end. A factual, guidance-side **surprising-lever callout** flags a plan that models living longer yet comes out with a *higher* chance the money lasts (the survivor-cliff signal). One home for the compared set: `CombinationComparisonData` (Compare render + CSV controller both use it). Tested: presenter chip/sparkline/callout, **neutral order when `personal_use=false`** vs ranked in advice mode, CSV disclaimer/figures/owner-scoping/base-centric; the banned-phrasing partition **passes in enforce mode** (neutral zone stays clean). See DECISIONS 2026-07-05 + docs/PLAN-decision-support.md. **Deferred fast-follows:** a chosen-subset selector (v1 compares the whole family); the sparkline's per-year figures as a full accessible table. **Pending Rob's browser sign-off** (needs completed family runs — the local DB has none; use Compare's "Re-run all N").
- **Done — decision-support Phase 4 (the survivor-cliff story, part of Phase 4) (2026-07-05):** `ResultPresenter::incomeFloor()` snapshotted only the **last all-alive year**, reading the floor *before* the first death and understating the survivor-poverty risk. It now also carries a **`survivor`** twin computed at the **deepest survivor year** (last year exactly one person is alive) off the same `YearResult`, plus the signed coverage **`cliff`**; a new private `floorAt(YearResult)` is the one definition both floors read (so the twin reconciles to the ladder's survivor rows). Null twin for a single person / no survivor phase. Rendered as a factual before/after **dumbbell** on the results-page income-floor section (and a PDF survivor line), guidance-side. Tested (twin reconciles to the forecast's survivor year; the cliff drops when a State Pension is lost; single → no twin; the results page renders it, no leaked Blade directive). See DECISIONS 2026-07-05.
- **Done — decision-support Phase 4, the first survivor lever: the survivor's-DB-share sweep (2026-07-05):** new engine `Sweep\Lever\SurvivorDbFractionLever` (+ an immutable `DbPension::withSpousePensionFraction`) sweeps the survivor's fraction (`spousePensionFraction`) 0–100% on the household's DB scheme(s). Three modelling calls: it **varies only schemes that already provide a survivor pension** (a null-fraction scheme is left untouched — the lever never invents a benefit); it is **monotone `Increasing` and CRN-safe** (more guaranteed survivor income only helps, and the change touches neither the mortality draw nor the return path, so the sweep's common random numbers hold and the crossing is a valid monotone fit — unlike the longevity/SP-deferral levers to come, which desync RNG and must declare `Unknown`); and it is **gated** in `ThresholdExplorer` to a couple (a survivor to inherit) with a DB scheme that offers a fraction. Wired through `LeverKey::SurvivorDbFraction` + `LeverThresholdService::buildLever`/`defaultGrid` (0–100% grid) + the presenter's value/caption arms; the outcome mapper needed no change (it keys the lever by its string value). Tested engine-side (moves only survivor-pension schemes, clamps 0–100, a bigger survivor pension does not lower success on a survivor-cliff couple) and app-side (default grid brackets 0–100, a real scenario computes the threshold, the explorer shows it for the couple fixture and hides it for a single person with no DB).
- **Done — decision-support Phase 4, the second survivor lever: the joint-life-annuity survivor-share sweep (2026-07-05):** the annuity twin of the DB lever. New engine `Sweep\Lever\SurvivorAnnuityFractionLever` (+ immutable `AnnuityPurchase::withSurvivorFraction` and `DcPension::withAnnuityPurchase`) sweeps the survivor's fraction (`AnnuityPurchase::survivorFraction`) 0–100% on the household's joint-life annuity. Same three calls as the DB lever: it varies **only annuities that are already joint-life** (a single-life annuity is priced on a single-life quote, so making it joint-life at the same rate would model income the quote never paid for — the annuitant's own income is held fixed, only the survivor's share moves); it is **monotone `Increasing` and CRN-safe**; and it is **gated** in `ThresholdExplorer` to a couple with a DC annuity that already provides a survivor fraction (the DB and annuity survivor levers gate independently). Wired through `LeverKey::SurvivorAnnuityFraction` + `buildLever`/`defaultGrid` (0–100%) + the presenter's value (grouped with the DB `%` arm) and caption arms. Tested engine-side (moves only joint-life annuities, clamps 0–100, a bigger survivor income does not lower success on a survivor-cliff couple) and app-side (grid brackets 0–100, a joint-life-annuity scenario computes the threshold, the explorer shows it for that fixture and hides it for the rich fixture which annuitises nothing). **The third — per-person longevity — followed (below); two remain** — see What's next.
- **Done — decision-support Phase 4, the third survivor lever: the per-person longevity sweep (2026-07-05):** splits the combined "live longer" bump *per person* so a sweep asks "whose longevity moves the odds?" New engine `Sweep\Lever\PersonLongevityLever` (+ immutable `Person::withLongevity`) sets ONE named person's `LongevityAdjustment` to a ±year `OffsetYears` (grid −5..+15, 0 = peer) on that person alone. It is **`LeverDirection::Unknown`** — genuinely non-monotone (extending the better-provided partner raises success, the survivor lowers it), so the sweep reports the first crossing and never fits a single monotone one. It is nonetheless **CRN-safe**: the offset applies *after* the peer death is drawn (`JointLifeSampler`/`RepresentativeDeathAge`), so the mortality draw count is unchanged across the grid (resolved the flagged RNG modelling call — `Unknown` stands on non-monotonicity, not RNG; a `FixedAge`/multiplier lever *would* desync, the offset lever does not). First **per-person-parameterised** lever: a `?string $leverParam` (person id) threads through `LeverThresholdService::buildLever`/`compute`/`deterministicForecastAt` + `ThresholdRunner::request`/`inputsHash`/`createRun`/`execute` into a new **nullable `threshold_results.lever_param` column** (additive migration) — it **joins the inputs hash** (one person's threshold is never served for another) and is kept **separate from `lever_key`** so that stays a clean enum value; the composite `person_longevity:p1` id lives only in the `ThresholdExplorer` menu (one entry per person, gated to a couple, "How long <person> lives"). This parameterisation is the shared foundation the defer-the-survivor's-SP lever reuses. Tested engine + app (offsets only the named person; +12y reaches the deterministic death year; default grid; Unknown-direction threshold; one lever per person for a couple, none for a lone person; find-limit records `lever_key`+`lever_param`; distinct cache key per person; a completed threshold paints "+8 years"). See DECISIONS 2026-07-05.
- **Done — builder: a free-text "Note" on each Other-income row (2026-07-05):** an optional label for what an income is and where it's from (e.g. "Aviva annuity", "buy-to-let flat in Leeds"), a **pure visual aid** — it round-trips through `builder_state` but `HouseholdAssembler` reads named keys only, so it reaches no `IncomeStream` DTO or engine figure. Empty default (records no spurious what-if delta, needs no `loadState` backfill), `nullable|string|max:120` validation, tested (persists yet the assembled `IncomeStream` has no such property). Applies to every row in the section, not just `type = Other`.
- **Done — age-varying spending "smile" (2026-07-06):** spend now varies with age per line (the promoted "next engine piece"). A `SpendPath` value object (piecewise-real `{fromAge, amount}` bands) is the one representation — chosen from an industry-norm survey (Voyant / Kitces-Basu per-category / RightCapital phases / Blanchett curve → per-item age-bounded breakpoints; docs/RESEARCH-under-spending-smile.md). `ExpenseProfile` holds essential/discretionary as `SpendPath`s beside the headline scalars (scalar == first band, enforced — one home); `HouseholdAssembler` sums each line's `builder_state.expenseLines[].bands` into the aggregate paths (reconciled: aggregate == Σ line paths at every age); `PathProjector` charges at the reference (first-declared) person's age, so the MC + `HistoricalBacktester` inherit it. A **flat plan is byte-identical** to the pre-smile engine. Builder **band editor** (per always-condition spend line; sparse, so a no-smile line / an unchanged what-if records no delta) + a results **input-sanity note**; the cashflow ladder / live preview / PDF / lever-explorer reflect it automatically (they read per-year spend). Guards: `SpendPathTest`, `SpendingSmileTest`, `SpendingSmileAssemblerTest`, `ScenarioBuilderSmileTest`, `InputNotesTest`. See DECISIONS + DATA-MODEL 2026-07-06. **Pending Rob's browser sign-off** (add an age band to a discretionary line in the Spending step, open the results page — the ladder should step down). **v1 limits (flagged):** bands key off person 1's age; only always-lines smile (contingent costs stay flat); the late-life rise is the separate care spell, not a band. **Fast-follows:** the "hand-draw the smile" editor + %/yr-and-phase templates; what-if delta **highlighting** of band changes.
- **Done — decision-support Phase 6: the assistant states computed limits, hash-gated (2026-07-07) — the decision-support feature is COMPLETE (Phases 0–6):** `App\DecisionSupport\ThresholdFacts` feeds the scenario's fresh computed limits (1-D thresholds, the trade-off map, the care before/after) into the assistant's `ScenarioContext` as facts — only rows whose `inputs_hash` still matches a hash recomputed against the current effective form-state qualify (stale rows are muted even if they survive the delete-on-edit invalidation), so G1 grounds the fresh band and mechanically refuses the same sentence once stale. One wording home per limit (the meter caption / the frontier summary / the care comparison), neutral phrasing asserted on the fact text, an honest "none computed yet" fact otherwise. The context also volunteers the **survivor cliff** (income-floor + survivor-twin facts) with new starter questions ("What happens to the survivor's income…", a "How far can we go?" group). See DECISIONS 2026-07-07. **Pending Rob's browser sign-off** (enable the assistant, compute a limit, ask "what limits have been computed?"; then edit an input and ask again — it must decline the old figure).
- **Done — decision-support Phase 5: the 2-D trade-off map (2026-07-07):** the buy-price ceiling at each held retirement age, rendered as a **success heatmap whose tint boundary IS the iso-line** (every measured cell shown; `FrontierPoint` carries its full per-column curve, so the crossing provably equals `findCrossing` of the cells). Rides the same `ThresholdResult` record (additive `condition_lever_key`/`condition_grid` discriminator columns, both in the inputs hash) so invalidation/progress/cancel/CSV came free; queued at 1,000 paths/cell over a coarse 5-value condition grid (cost bounded by construction). UI on the "How far can we go?" panel: a both-levers-pinned summary ("up to about £X with retirement at age A, and up to about £Y at age B"), per-column banded chips, analyst tinted-cell table + frontier CSV. Guards: engine (curve carried + crossing consistency + per-cell progress), the byte-identical column-vs-1-D pin, runner round-trip + distinct hashing, presenter/CSV/explorer tests. See DECISIONS 2026-07-07. **Pending Rob's browser sign-off** (open a buy scenario's results, "How far can we go?" → "Map the trade-off"; needs the queue worker). **Remaining in the feature: Phase 6 (assistant tie-in).**
- **Done — equity-release lifetime mortgage modelled + two V2 what-ifs (2026-07-06):** a new nullable `Property::mortgageRollUpRate` makes an unpaid lifetime-mortgage balance **roll up** — compounding at its fixed nominal rate in `PathProjector::growState`, **NNEG-capped** at the home value, feeding the IHT estate; a reconciled `YearResult::mortgageBalance` + `netWealth()`/`homeEquity()` keep the debt visible (gross `totalWealth` would flatter a roll-up — the V2 base reads £530k gross vs £212k net; **superseded 2026-07-08:** `totalWealth` itself is now the net figure and `netWealth()` is gone — see the net-wealth Done bullet). Fills the flagged equity-release gap; guarded by `LifetimeMortgageRollUpTest` + two `InputNotesTest` cases. Two Stay-put delta-children — **roll-up (no payments)** vs **interest-serviced** — both at 6.5%: the deterministic read is the roll-up frees ~£7,080/yr so the money never depletes (the base depletes 2045) but cuts the estate ~£371k → ~£201k. (A third combining the roll-up with part-time work + a child's contribution followed on 2026-07-08 — Session log.) See DECISIONS + DATA-MODEL 2026-07-06. **Pending Rob's browser sign-off** (the 2026-07-07 family re-run covered the Monte Carlo).
- **Done — the assistant turn is queued to the worker (2026-07-08, from Rob's browser 504):** asking the panel a long question died as a raw nginx 504 — the synchronous v1 ran the whole local-model generation inside one Livewire request (up to 120s × 2 guarded attempts), past the web server's 60s gateway default. `ask()` now persists a **transient, encrypted `assistant_turns` row** (question + history) and dispatches `RunAssistantTurn` (tries 1, timeout 300s); the panel **polls at 1.5s** exactly as the results page polls a run (same `SimulationStatus` lifecycle + the same awaiting-worker hint, so a missing worker is loud), then **deletes the row the moment its answer joins the browser-local transcript** (no server-side transcript; Clear wipes leftovers; user/scenario deletion cascades). The context assembly moved intact from the component to `App\Assistant\AssistantTurnRunner`; advice mode is resolved per turn via `Gate::forUser` on the turn's owner (a worker has no session); guards G1/G2 untouched. **Assistant answers now need the queue worker running.** A per-site Herd nginx conf (machine config, see How to pick up) also raises this site's gateway timeout to 300s for the remaining synchronous call (the Ideas capture). Panel tests cover the queue-and-poll round trip (incl. the unreachable-model path), inert-when-disabled, Clear deleting rows, and the owner-scoped poll. See DECISIONS 2026-07-08. **Pending Rob's browser re-ask** (worker running). NB the question that 504'd — "build me a what-if" — is a capability the assistant deliberately lacks: that is the approved-but-unbuilt [docs/PLAN-assistant-scenario-editing.md](docs/PLAN-assistant-scenario-editing.md); the requested what-if was hand-built as an ordinary delta-child instead (Session log).
- **Done — all reported wealth is NET of the mortgage (2026-07-08 evening, from Rob's Compare question):** "which plan leaves the most money?" crowned the LTM roll-up combination at £595,694.63 because every total-wealth surface added the home **gross** — the rolled-up debt was tracked per-year (`mortgageBalance`, 2026-07-06) but never reached the terminal headline, the MC percentiles/fans, or any display. Rob's ruling: gross is irrelevant ("she can't eat the building"). `YearResult::totalWealth` is now **derived in the constructor** as liquid + pension + home equity (NNEG-floored, the `EstateValuer` definition); the then-identical `netWealth()` deleted; `terminalTotalWealth` / Simulator / Compare / PDF / CSV / assistant facts / builder preview inherit from that one home; labels read "incl. home equity"; `ENGINE_VERSION` → `finance-engine/phase-3-net-wealth`. Guards: `WealthReconciliationTest` (parts-sum ± mortgage **and the debt reaching the terminal headline** — the missing assertion that let this live) + `LifetimeMortgageRollUpTest` headline pins. Corrected V2 read: best total = "YCC works to 72 + full SP £241/wk, no disability benefit" £479,604.55; best spendable = "Sell (repay £208k) & buy cheaper £165k" £172,613.70; the old "winner" is really £277,187.58. **Also (same session): all 15 what-if children renamed** so each name states its modelled changes (the worst offender modelled no letting at all) — names went into `overrides['name']` + `projectFrom()`, no runs invalidated; cached thresholds re-key (name is in the inputs hash — open refinement). See DECISIONS + DATA-MODEL 2026-07-08. **Two input questions for Rob** sit in the Session log (taxable child-help; the £208k figure).
- **Done — care years are means-tested in the projection (2026-07-08 late, first What's-next-#3 refinement):** the gross-self-funder-fee flag is closed — each care year is charged at what the **household bears**. `CareMeansTest::annualCharge()` is the one home for the charge rule (`min(fee, capital above the £23,250 limit + tariff + income − PEA)`); `PathProjector` assesses **per resident** (England's individual assessment: their own accounts + own taxable income; pension pots disregarded as capital; the home only when the resident lives alone or it is let, PC-mirroring, split for a couple); the **PEA is a new sourced figure** (£30.65/£31.80 per week 2025-26/2026-27, DHSC LAC circular, verified 2026-07-08) and uprates with inflation while the capital limits stay frozen. `careCostReal`/`CareImpact` are now the household-borne bill (panel relabelled "Chance care costs you something"); `ENGINE_VERSION` → `finance-engine/phase-3-care-means-test`. Penny-exact guards: `CareMeansTestTest` (formula) + `CareMeansTestedChargeTest` (projection: contribution-not-fee, spend steps by the charge, self-funder preserved, lone-owner home assessed, partner shields the home, nothing-of-their-own → fully LA-funded). v1 flags in DECISIONS 2026-07-08. **Pending Rob's browser sign-off** (care panel copy + figures).
- **RESOLVED — the queued Monte Carlo reproducibility bug is fixed by moving to Postgres (2026-07-09).** Stored MC runs from the `queue:work` daemon used to recompute to a different success probability at their own seed (up to 14 points, intermittent). Root cause: **SQLite could not handle the `database` queue driver's concurrent access.** Fix: **the app database is now Postgres 18** (`.env` `DB_CONNECTION=pgsql`, db `retireforecast`, `postgres`/`postgres`). The whole family was re-run through the identical `queue:work` path on Postgres and every plan reproduces exactly (all-0.0 gaps). In-app "Re-run all" is trustworthy again. The investigation proved the engine itself is deterministic and correct throughout; only the SQLite+queue combination was at fault. See DECISIONS 2026-07-09 (+ the "How to pick up" DB setup below).
- **Done — a let property's mortgage interest gets the BTL finance-cost tax reducer (2026-07-09, from Rob's "does #17 count the rent?" question):** rental income was counted, but taxed with no relief for the interest. `PathProjector` now cuts household tax by **20% × min(mortgage interest, rental income)** when `isLet` (the April-2020 finance-cost restriction), capped at tax due; `rentalIncomeNominal` counts only `Rental` streams. #17's rental set to **£1,800/mo (£21,600/yr)**. Together these move #17's deterministic depletion 2030 → **2035** (still doesn't fully last — letting keeps the £208k mortgage AND pays rent elsewhere). `ENGINE_VERSION` → `finance-engine/phase-3-btl-finance-cost` (family re-run). Guard `BuyToLetFinanceCostTest`. v1 flags + the "utilities are not over-charged (reconsidered)" note in DECISIONS 2026-07-09. **Pending Rob's browser sign-off.**
- **Done — a bought home carries standard 1% maintenance (2026-07-08 later, from Rob's buy-plan-costs point):** the buy variant scaled the current home's `runningCosts` to the new home, but the V2 flat's `runningCosts` is empty (its upkeep is inside the stripped service charge), so a bought freehold inherited **zero** upkeep. `HousingComparison::newHomeRunningCosts` now applies a sourced **1%-of-value** maintenance default (Checkatrade UK rule of thumb) when there is no scaled basis; a real property's costs override it. `ENGINE_VERSION` → `finance-engine/phase-3-home-maintenance`, so the 16 stored runs are stale (re-run pending the £208k fix below). Deterministic impact: the buy plans drop sharply (e.g. "buy cheaper £165k" spendable £172,614 → £135,209; "buy near kids £260k" £65,751 → £15,018). DECISIONS + DATA-MODEL 2026-07-08. **Pending Rob's browser sign-off.**
- **Done — plan #23 child-help is tax-free (2026-07-08 later):** Rob confirmed no work is exchanged for the child's £300/mo → a gift, not income → `taxable=false`, plan renamed "…(tax-free)". A gift is disregarded for income tax, Pension Credit and the care means test, so the taxable flag had understated the plan three ways. DECISIONS 2026-07-08.
- **NOT A BUG — the base's £118k mortgage is deliberate (my 2026-07-08 error, reverted).** I wrongly flagged the stay-put plans' £118k as a bug and briefly changed the base to £208k; **reverted**. Per `docs/SCENARIO-V2.local.md` (Rob's 2026-07-03 call): the flat's real mortgage is a **£208k BTL** they cannot keep (living in a BTL is the breach; called for redemption Dec 2026), so **"Stay put" models paying ~£90k down to £118,000 and converting to a residential RIO at 6% = £7,080/yr** (the £90k assumed from outside modelled assets, for a clean base; the "keep £208k + £90k one-off" alternative was explicitly rejected). Disposal plans correctly use £208k (sell clears it; let keeps the BTL + £16,170.96/yr). **Lesson: read `docs/SCENARIO-V2.local.md` before touching any V2 figure** — it is the design record, not just a rebuild seed. Rob's current-mortgage confirmation (£208k BTL, £1,347.58/mo) matches the model's starting point; nothing to fix unless Rob revisits the paydown design.
- **Done — home-ownership costs can outpace inflation (2026-07-08 later, from Rob's service-charge challenge):** `ExpenseProfile::propertyCostsRealGrowth` compounds the while-owning-home bucket (service charge / ground rent / levies) at a REAL rate on top of CPI; follows the bucket (sell variants / a forced sale escalate nothing; the contractual mortgage payment is untouched). Builder input on the Spending step (`expense.propertyCostsGrowthPct`, sparse — no spurious what-if deltas), results-page input note, penny-exact guards (`PropertyCostsGrowthTest` + assembler completeness + sparse round-trip + note test). **The V2 base is set to 1.5%** (the last-3-years real rate — Rob's "assume the worst" ruling; his 12-year history actually ran ~0.5%/yr BELOW CPI, the analysis is in DECISIONS 2026-07-08) and the family re-run. **Pending Rob's browser sign-off** (Spending step field + the input note).
- **In progress:** nothing mid-edit. One live carry-over a fresh agent must know: the real **V2 couple's data** is captured privately in the gitignored `docs/SCENARIO-V2.local.md` (never commit) — the durable source to rebuild the scenario after a DB wipe.
- **Known bugs / broken:** none open (the five 2026-06-28 re-review findings are all resolved — see docs/PLAN.md "Review findings" + DECISIONS). Documented v1 scope limits, all flagged in code: income tax England/Wales/NI only (Scotland throws); emergency tax models the over-deduction magnitude, not PAYE-table pennies; mortality grid ages 50–100 / years 2025–2074 with clamping + a non-ONS tail above 100 (cap 110); forecast taxes GIA dividends + cash interest annually AND realises CGT on GIA disposal (ISA tax-free; GIA/cash grow at capital only; v1 omits capital-loss relief + judges the CGT band on non-savings income); income-tax thresholds frozen until 2031, then indexed with inflation; DB escalation + triple lock as smooth growth factors; selling a **let** former home is charged **partial-PRR CGT** (occupation-driven, joint-owner split, gov.uk HS283 — see DECISIONS 2026-06-30), with deemed-occupation absences entered by hand and one rate per owner; a home lived in throughout stays full-PRR / £0; no SDLT surcharge on the replacement; house/salary growth deterministic inside the Monte Carlo; a **one-off cost fires on the first-declared person's age only** (`oneOffCosts` has no `personId` — a second-person-age cost can't trigger); a **repayment mortgage's balance is modelled static** (interest/payments are expense lines; no amortisation schedule) — under net-wealth reporting (2026-07-08) an un-redeemed balance visibly reduces total wealth to the end, so set `mortgageRedemptionYear` + repay-from-capital to clear it (true amortisation would need the mortgage rate as an input). **New v1 limits from the 2026-07-02/07-03 silent-drop fixes** (all flagged in code + DECISIONS): DB **commutation** treats the lump sum as fully tax-free (the LSA cap is not enforced) and a member dying before NRA never takes it (the survivor then uses the reduced base); **ownership share** apportions the mortgage + running costs pro-rata (a non-pro-rata upkeep split isn't modelled); **NI category** covers the employee-rate profiles (standard / reduced / deferred / nil) but not employer NI. The five collected-but-unconsumed inputs the 2026-07-02 audit found are now all consumed (DATA-MODEL "Known divergences" — no open silent-drops).

## What's next (in order)
The adviser-legibility workstream and the whole post-v1 backlog are built (see Current state). What remains:
1. **Rob's browser verification + sign-off** (testing deferred by Rob). The whole post-2026-06-29 cluster is built but unreviewed in the browser. **Re-run the browser a11y pass over the post-06-29 panels** (the 2026-06-29 axe pass predates annuitisation / stress-test / care-risk / sequencing; `npm run a11y` for the CI sweep; see docs/A11Y.md); check the **mobile** view of the results "on this page" nav (desktop signed off); the **2FA QR scan**; eyeball the new panels (annuitisation / stress-test / care-risk / withdrawal-sequencing, the IHT panel — toggle "Model inheritance tax" on in the builder, set the relationship, open the results page; the panel is deterministic so it shows pre-run — the spending-smile band editor + stepped ladder, **and now the decision-support finishers**: the **trade-off map** on "How far can we go?" (a buy scenario, "Map the trade-off", needs the queue worker; does it reproduce the V2 pair?) and the **assistant stating computed limits** (enable the assistant, compute a limit, ask "what limits have been computed?", then edit an input and confirm it declines the old figure)). **NB thresholds, the trade-off map AND assistant answers (queued worker turns since 2026-07-08) all need the queue worker running** (the V2 family's stored runs are already on the net-wealth engine — re-run 2026-07-08). The scripted Pa11y smoke re-ran green 2026-07-08 with `/methodology` added (docs/A11Y.md); what remains of the a11y item is the in-browser axe DevTools pass over the authenticated panels.
2. **Public-release blockers** (harmless while private; mandatory before any public launch — each flagged in code). Set **`config('compliance.personal_use')` false** + confirm the guidance-only partition re-applies (the suite already runs with it false); **swap the stress-test dataset** off the CC BY-NC-SA JST source for an OGL/licensed one (`HistoricalReturns`, DECISIONS 2026-07-01); tighten the **CSP `script-src` to nonces** (Alpine CSP build, needs the browser); complete the a11y pass to a public bar.
3. **Optional refinements to built features** (all flagged v1 limits; pick by value). **Care:** sex/age-split probability + HSLE timing, ONS-xlsx auto-parse for `mortality:refresh`; means-test v1 flags (count Pension Credit into the contribution, an LA fee rate below the self-funder rate, the both-in-care home disregard) — the tail itself is means-tested since 2026-07-08. **CGT:** deemed-occupation absences, per-owner band-straddle from exact income, shared-occupancy lettings relief (DECISIONS 2026-06-30). **Monte Carlo:** stochastic house/salary growth (currently deterministic), post-2031 reindexing, per-scheme DB escalation. **Annuitisation:** explicit retirement-*month* override.
4. **CI / data hygiene (remainder).** The freshness guardrails now run monthly in CI (the `data-freshness` workflow, DECISIONS 2026-07-08 — it takes effect on GitHub only once pushed). Remaining, low-value hardening (confirm worth it first): tamper-evident run hash, forecast caching.

**Specced-but-unbuilt work (lanes closed — no coordination needed, just pick it up).** **Decision-support: lever thresholds + combination comparison** (docs/PLAN-decision-support.md, 2026-07-04, 4-agent-reviewed) — **Phase 0 (the headless `SweepEngine` correctness spine) BUILT + the real levers + the Phase 1 COMPUTE CORE (2026-07-04):** `packages/finance-engine/src/Sweep/` (S1 MC-vs-deterministic guard, S2 2-D frontier, S3 banded crossing verdicts, Wilson CIs, pinned seed; `SweepEngineTest`); the real levers `Sweep/Lever/` (retirement-age / essential-spend / buy-price — the buy-price one reuses `HousingComparison` so a threshold reconciles with the results page; `SweepLeversTest`); and `App\DecisionSupport\LeverThresholdService` (+ `LeverKey`, `ThresholdOutcome`) — a scenario → threshold bridge via the single `ScenarioForecaster`, fixed seed, default per-lever grids, `onProgress` hook (`LeverThresholdServiceTest`). Rob resolved two open questions (Q3 target: sweep both essentials + full-spend, engine parameterised; Q2 family-money × PC: **researched** — disregarded income per DWP, DECISIONS 2026-07-04). **Phases 1 + 2 + 3 are now COMPLETE (2026-07-05)** — Phase 1 = the queued/persisted/cacheable `ThresholdResult` backend (`RunLeverThreshold` job + `ThresholdRunner` + `ThresholdOutcomeMapper`, inputs-hash keyed, two-layer edit-invalidation, owner-scoped CSV); Phase 2 = the **"How far can we go?" results panel** (`App\Livewire\ThresholdExplorer` + `App\DecisionSupport\ThresholdPresenter`); Phase 3 = the **combination-comparison surface** on the Compare page (`App\DecisionSupport\CombinationComparison` + `CombinationComparisonData` + `CombinationCsvExporter`: word-band chips + net-position sparklines over the base + its ready what-ifs, **unordered in guidance mode, best-first ordering + narrative behind the `interpret` gate** via `Interpretation::combinationRanking()`, factual surprising-lever callout — see Current state + DECISIONS 2026-07-05). **Phase 4 is COMPLETE (survivor-cliff story + all 5 levers):** the **survivor-cliff story** (`ResultPresenter::incomeFloor()` survivor-year twin + the before/after dumbbell), and the full lever menu — **DB survivor fraction** (`SurvivorDbFractionLever`) + **joint-life annuity survivor %** (`SurvivorAnnuityFractionLever`) (both monotone `Increasing`, CRN-safe), **per-person longevity** (`PersonLongevityLever`, the first **per-person-parameterised** lever — `lever_param` column + composite menu id, `Unknown` + CRN-safe), **defer the *survivor's* State Pension** (`StatePensionDeferralLever`, per-person, `Unknown` + CRN-safe — required a **State Pension deferral correctness fix**: deferral now delays the claim/forgoes income instead of a free uplift, and lands the "survivor's-SP raises the floor, first-dier's does not" headline test), and **care on/off pinned** (`CareModellingLever`, the first **settings-flipping** lever — flips `ForecastSettings::modelCareCost` over a [0,1] grid; a genuine **pin-and-compare, not a sweep** since care-on desyncs the return path, so the app **branches** to a two-state before/after with no slider/meter/S-curve/crossing; DECISIONS 2026-07-06). **Phases 5 + 6 are BUILT (2026-07-07** — the 2-D trade-off map, then the hash-gated assistant tie-in; see Current state + DECISIONS 2026-07-07), so **the decision-support feature is COMPLETE (Phases 0–6), pending Rob's browser sign-off.** Q1 (public-build ordering) is still Rob's, but now that Phase 3 shipped it only refines Phase 3's *ordering* posture at public release (the gate already re-hides it). Lane C: withdrawal-sequencing next steps #5/#6 (docs/PLAN-withdrawal-sequencing.md — gated on two modelling calls from Rob). Lane D: multi-property (docs/PLAN-multi-property.md, DRAFT, [needs Rob]). (Lane B is complete — the in-place forced sale built 2026-07-03; **IHT + relationship status is now built** — see Current state + DECISIONS 2026-07-04.)

## Open items
Open decisions and parked work, off the immediate go-live path (which is under What's next).
- [ ] **Spreadsheet import** — the line-item expense-categories data-model decision; re-verify IWT CSP vs the real 2023 export (the fixture was built from a masked dump); Nischa deprioritised (`isAvailable()=false`); imported income → Person 1, no start age (by design, flagged). Real sample `.xlsx` live in gitignored `docs/*.xlsx` (never commit).
- [ ] **Assumption-set figures** are not numerically editable in Filament (curate-metadata-only; figures seeded from the engine library — editing one means re-sourcing with a new verified-on date). **Now subsumed by the "everything user-editable" direction** (DECISIONS 2026-06-29 + What's next #3): assumptions become editable in the **main UI** as a user-derived *custom set* from the sourced presets, not in Filament.
- [ ] **ONS mortality + FCA/DMS assumption *sources*** sit at their 2026-06-24 sign-off (docs/ASSUMPTIONS.md, docs/MORTALITY.md) — a separate review from the gov.uk statutory-figure pass; the `investmentIncomeYield` 2% is a reviewed-and-kept modelling assumption.
- [ ] **Demo couple's anonymised figures** — Rob supplies later, entered via the UI, not hardcoded (field list in docs/PLAN.md "Data Rob supplies").
- [ ] **External-review enhancement backlog** (post-v1, not blocking) — docs/PLAN.md "External review triage" (cashflow timeline, longevity-distribution visual, stress-test panel, what-if sliders, v2 annuitisation + care-cost stochasticity). Declined items recorded in DECISIONS 2026-06-25.
- [ ] **Delta-research backlog (2026-07-02)** — docs/PLAN.md "Delta-research backlog" + docs/RESEARCH-delta-2026-07-02.md. The **data-integrity fixes are all done** (the five collected-but-unconsumed inputs; DATA-MODEL "Known divergences" — survivor DB pension, per-person salary growth, DB commutation, NI category, ownership share; 2026-07-02/07-03). Remaining: the user-facing-copy fixes are now **done** (the "Survivor fraction" field + the **care-off silence** — a "care isn't modelled" heads-up landed 2026-07-04), then the uncertainty-communication + household-composition + methodology + a11y items in the Delta-research backlog. Then, by value: uncertainty-communication upgrades; the **third-adult-contributing-to-upkeep** scope item (Rob's back-burner ask — BoardContribution stream + HouseholdMember, no third planning subject); a user-facing **/methodology** page; an adviser/Pension-Wise output pack; **WCAG 2.2 AA** + mobile to a public bar; and the JST-stops-at-2020 stress-test data gap. **Household scope decided: no third full planning subject** (DECISIONS 2026-07-02).
- [ ] **The under-spending case (2026-07-06)** — docs/PLAN.md "The under-spending case". Backlog from an FCA-planner case study (which validated our IHT to the penny + confirmed the historical-stress-test method): promote **phased spend / the "smile"** to the next engine piece (an `ExpenseProfile` age-banded path — also unblocks the "hand-draw the smile" editor and a full reproduction of the video, and fixes a flat-real bias that *understates* safe early spend), a net-new **lifetime-gifting lever** (PET / 7-yr taper; both a decision-support lever and an IHT-completeness item), and an **under-spender headline reframe**. Withdrawal-rate population context is note-only (the diagnostic was declined 2026-06-30).
- [ ] **In-app local-model assistant (2026-07-03)** — **ALL THREE PHASES BUILT** (see Current state; pending Rob's browser sign-off). docs/RESEARCH-local-assistant.md + docs/PLAN.md "In-app local-model assistant" + DECISIONS 2026-07-03. A local (Ollama) results-page "chatbot" that **explains + captures, never predicts/calculates/builds**: (1) ✅ grounded scenario-explainer (also on the **Compare page**, comparison-aware via a shared `AssistantContext` + `ComparisonContext`), (2) ✅ methodology doc-RAG (curated corpus + docs/METHODOLOGY.md / the `/methodology` page — see LA-9), (3) ✅ idea capture — the "Ideas" tab, the model's **only** write (`assistant_backlog_items`, append-only + attributed + reversible; it never builds; `assistant:backlog` lists the queue for human promotion). Two guardrails — G1 figure-grounding, G2 runtime phrasing partition — the latter a flagged public-release blocker. **Remaining = Rob's browser sign-off + optional fast-follows** (streaming, per-plan Monte Carlo on Compare, a side-nav entry, the /methodology-page enhancements in PLAN).

## How to pick up
Run from the **project root** (the test runner shells out to a relative phpunit path, so it fails from `C:\Users\r`):
```powershell
Set-Location "C:\Dev\RetireForecast"
# NB: php / artisan / composer / npm are NOT on the Git Bash PATH on this machine — run them via the
# PowerShell tool (PHP 8.4 is provided by Laravel Herd). Bash is fine for git / grep / file ops. See CLAUDE.md.
php artisan test                            # full suite — must be all green (the baseline; red = stop and fix)
php artisan test --testsuite=Engine        # engine only
npm run a11y                                 # Pa11y CI a11y sweep (needs the served app — see docs/A11Y.md)
vendor/bin/pint --dirty                      # house style on changed files
npm run build                                # build assets (public/build is gitignored); `npm run dev` to watch
```
**Queue worker — run it with JIT for the 10k Monte Carlo (≈2× faster, byte-identical).** OPcache JIT is off by
default on this machine (`opcache.enable_cli=0`, `opcache.jit=disable`), so the full simulation job runs fully
interpreted unless you start the worker with JIT:
```powershell
php -d opcache.enable_cli=1 -d opcache.jit_buffer_size=128M -d opcache.jit=1255 artisan queue:work
```
The synchronous preview (deterministic, 1 path) does not need it. Re-measure perf with a 10k `Simulator::run` over
the `comfortable` fixture (see DECISIONS 2026-06-28 "10k-path perf"); there is no timed test (wall-clock is
environment-dependent), the `IncomeTaxTotalPenceTest` grid is what guards the refactor's correctness.
If `vendor/` is missing: `composer install`. If engine classes are not found, re-register the path package: `composer update retireforecast/finance-engine`. To use the app locally: **Herd already serves it at `https://retireforecast.test`** — no `php artisan serve` needed locally (run `npm run build` after asset changes, as `public/build` is gitignored). `php artisan serve` is only needed where Herd is not running (e.g. CI). This flow works fine under the new CSP. **CSP note:** the `web` group ships a Content-Security-Policy (`config/security.php`); it does **not** include the Vite dev-server/websocket origins, so if you use HMR (`npm run dev`) set `SECURITY_HEADERS_ENABLED=false` in `.env` while developing, or `SECURITY_CSP_REPORT_ONLY=true` to stage it. **DB setup — IMPORTANT (Postgres since 2026-07-09):** the app now runs on **local Postgres 18**, not SQLite (the move fixed the queued-Monte-Carlo reproducibility bug — DECISIONS 2026-07-09). `.env` has `DB_CONNECTION=pgsql`, db **`retireforecast`**, host `127.0.0.1:5432`, user/pass `postgres`/`postgres` (the pre-move sqlite line is kept commented for revert). On a fresh machine: create the db (`CREATE DATABASE retireforecast`) then `php artisan migrate --seed`. The old `database.sqlite` (+ a `.bak` copy) is retained but no longer used; the one-off `copy-sqlite-to-pg` migration script lived in the session scratchpad. **Tests still run on in-memory SQLite** (phpunit.xml env), so `php artisan test` is unaffected by the app DB. `--seed` runs `DatabaseSeeder` → assumption sets + a `test@example.com` / `password` user. For a ready-made fictional sample, seed the **demo preset**: `php artisan db:seed --class=Database\Seeders\DemoScenarioSeeder` (idempotent; outside production it creates **`demo@example.com` / `password`** with a base plan + one what-if child; in production set `DEMO_USER_EMAIL` to an existing user). Register your own at `/register` and **accept the one-time guidance-only disclaimer at `/welcome`**. **Two-factor auth** enrols at **`/account/security`** ("Security" in the nav; behind a fresh password confirmation). Build a forecast at `/scenarios/create`, run it on its results page; download a **PDF summary** from there. The full queued run needs a worker (`php artisan queue:listen`); the preview does not. **Admin panel at `/admin` is gated on `is_admin`** (default false) — grant yourself once with `php artisan user:make-admin {email}` (e.g. `demo@example.com`), after which the Users resource toggles **Admin access** + `can_interpret`. Tests neutralise Vite, so they pass without a build. **Local-model assistant (inert by default):** set `ASSISTANT_ENABLED=true` in `.env` to show the results-page side panel (needs Ollama up with `qwen3:14b` — already on this machine), **and the queue worker running: since 2026-07-08 answers are generated as queued worker turns** (without a worker the panel says so after ~15s rather than hanging). For Phase 2 methodology answers, build the doc index once with **`php artisan assistant:index-docs`** (curated corpus → gitignored `storage/app/private/assistant/doc-index.json`; without it, methodology just isn't attached and scenario answers still work). Re-run the command after editing a curated methodology doc. **Machine config (not in the repo):** `~/.config/herd/config/valet/Nginx/retireforecast.test.conf` raises this site's nginx gateway timeout to 300s (for the remaining synchronous model call and any other slow request); delete it to fall back to Herd's catch-all, then restart Herd's nginx.

## Sibling docs
| Doc | Purpose |
|-----|---------|
| docs/PLAN.md | The full approved implementation plan. Source of truth for scope, data model, tax rules, Monte Carlo design, phasing. Holds the "Sector-informed build plan (2026-06-25)". |
| docs/RESEARCH-cashflow-modelling.md | How the sector (Voyant/Timeline/CashCalc, PLSA/SMPI) solves edit/clone/compare, line-item expenditure, drill-down — what we adopt + the gaps it surfaced. |
| docs/RESEARCH-editable-assumptions-ux.md | How free consumer tools (Boldin, ProjectionLab, NYT rent-vs-buy, Guiide, Actuaries Longevity Illustrator) handle editable assumptions, buy-vs-rent and cost breakdowns — what we adopt (2026-06-29). |
| docs/RESEARCH-document-import.md | PARKED post-v1 feature: statement-driven onboarding + document import (sector evidence, document→builder-field map, gotchas). |
| docs/RESEARCH-competitive-gap-analysis.md | Full-market competitive scan (2026-06-30): where the engine already leads vs where the gaps are (decumulation policy + framing). Net-new backlog items folded into docs/PLAN.md "Competitive gap analysis". |
| docs/RESEARCH-delta-2026-07-02.md | Delta research (2026-07-02) over the five topics the competitive scan left as residuals/gaps: uncertainty communication, household composition (+ third-adult-contributing-to-upkeep UK mechanics), adviser/Pension-Wise outputs, accessibility + mobile (WCAG 2.2 AA), methodology disclosure. Adversarially source-checked; recommendations folded into docs/PLAN.md "Delta-research backlog". |
| docs/RESEARCH-local-assistant.md | Spec + feasibility (2026-07-03) for the in-app **local-model assistant** ("chatbot"): a results-page explainer + backlog-capture on a local model (Ollama). The model **explains + captures, never predicts/calculates/builds**; phased explainer → doc-RAG → capture; two guardrails (figure-grounding + runtime phrasing partition). **Phase 1 BUILT** (`App\Assistant\` + `ScenarioAssistant`); Phases 2–3 specced. See DECISIONS 2026-07-03. |
| docs/RESEARCH-stress-test-and-official-sources.md | Stress-test industry standards + official UK data sources (2026-07-01): historical sequence backtesting; BoE has no equity total return, so the source is the JST Macrohistory dataset (CC BY-NC-SA); care-cost = LaingBuisson/PSSRU + ONS timing; ONS-refresh = fully ONS. Source decisions resolved + built. |
| docs/PLAN-withdrawal-sequencing.md | Tax-efficient withdrawal sequencing across wrappers (ISA/SIPP/GIA) + "fill the band" with the lifetime-tax £-delta. **CORE SHIPPED 2026-07-01**; #5 PCLS-timing + #6 optimiser handed off (ready-to-execute plan in the spec, gated on two modelling calls from Rob). |
| docs/PLAN-mortgage-payment-stop.md | Spec + **BUILT** (2026-07-01): stops the bundled mortgage *payment* after a repay-from-capital redemption via a `while_mortgaged` expense condition + `ExpenseProfile::mortgageCosts`. Kept as the build record. |
| docs/PLAN-in-place-forced-sale.md | Spec + **BUILT** (2026-07-03): `ForcedSale` sold in place at the redemption year — `PathProjector` event sells at the grown value via the shared `HousingProceeds::compute`, frees equity into GIA, stops housing costs, charges the entered rent; rent + selling costs ride on `ForecastSettings`. The last Lane-B item, now closed. |
| docs/PLAN-iht-and-relationship-status.md | Spec + **BUILT 2026-07-04** (six green slices). Wired Inheritance Tax into the forecast (consumes the `ihtModelled` toggle) + `Household::relationshipStatus` (married/civil-partner vs cohabiting) driving the spousal exemption + transferable nil-rate band, `Iht\EstateValuer` + `Iht\IhtOutcome` on `ForecastResult`, a `homeToDescendants` toggle, results/Compare/PDF panels, and cohabiting survivor caveats. Kept as the build record + the flagged v1 limits (Monte-Carlo IHT distribution, gifts/trusts/reliefs). See DECISIONS 2026-07-04. |
| docs/PLAN-decision-support.md | Staged plan for the "lever thresholds + combination comparison" feature. **ALL PHASES (0–6) BUILT — feature complete** (Phase 0 spine 2026-07-04; Phases 1–3 + the Phase-4 survivor-cliff story + the full 5-lever menu 2026-07-05/06; Phase 5 the 2-D trade-off map + Phase 6 the hash-gated assistant tie-in 2026-07-07). Q2 (family-money × PC) + Q3 (target) resolved; Q1 (public ordering posture) waits on any public release. Hardened by a 4-agent review. Kept as the build record. |
| docs/PLAN-assistant-scenario-editing.md | **SPEC (approved scope, not built), 2026-07-04.** The local assistant assembles a **reviewable what-if** from the reader's own stated changes to the base (conversational; Phase 1 value-edits, Phase 2 add/remove rows) + **gated base editing** (Phase 3, `can_edit_base` default off, orphan + stale-run surfacing). A deliberate **narrow widening** of the assistant's "never builds" rule (reviewable delta-child, no invented figures/outcomes — DECISIONS 2026-07-04). Mostly a new *producer* of the existing delta shape: reuses `BuilderStateDelta`/`QuickWhatIf`/`QuickWhatIfController`/`WhatIfChanges`; new guards C1 (input grounding) + C2 (closed target menu). |
| docs/SCENARIO-V2.local.md | **GITIGNORED / PRIVATE** (real couple's data, never commit): the durable capture of the V2 couple + core scenario (incomes, the flat, CGT history, base + what-ifs) to re-model from after a DB wipe. |
| docs/METHODOLOGY.md | User-facing engine-computation methodology (income tax, lump-sum shock, SP, SDLT/CGT/PRR, benefits, IHT, care, Monte Carlo, the year loop) + "what we don't model". One source, two homes: the public `/methodology` page AND the assistant's methodology corpus. Written from a code-grounded engine survey (2026-07-03). |
| PRD.md | Goal, success criteria, scope, non-goals, open questions. |
| DATA-MODEL.md | Canonical data shape; what is materialised in code today vs planned. |
| DECISIONS.md | Append-only decision log with rationale. |
| CLAUDE.md | Root orient tripwire + build/test conventions + "Doc hygiene" rules. |

## Branch status
On `master`. A GitHub remote exists (`origin` → github.com/RobertLCraig/RetireForecast). Check sync with `git rev-list --left-right --count origin/master...master` — the count is not transcribed here (it drifts; the 2026-07-02 history rewrite + force-push is recorded in DECISIONS 2026-07-02). **Pushing to `master` is gated and needs Rob's explicit go-ahead — do not push unprompted.** Otherwise commit directly to `master` (personal local-first project; no PR flow). **Re-check `git status` / `git log` before any commit or push.** The pre-rebuild prototype is tagged **`prototype-v1` (a8f1f68)**, the only recovery snapshot. For the commit history use **`git log`** (the source of truth — not restated here, where it would drift); the recent trajectory is in the Session log + DECISIONS.md.

## Session log
_Newest first. Keep only the recent live window here; older sessions are in `git log` + DECISIONS.md. Per-session figures are dated history and may stay._

_2026-07-09 (MC reproducibility bug found, root-caused to SQLite, FIXED by moving to Postgres)_ —
Rob's follow-up on #17's rental led to noticing a family figure swinging more than sampling noise
allows. A long investigation (many scratch scripts) proved the engine is deterministic and correct
(same seed → identical across 6 processes, JIT and no-JIT) and that every non-queue path — a direct
`execute()`, a CLI loop of all 16 runs — reproduces exactly (gaps 0.0); only runs through the
`queue:work` daemon were intermittently wrong (up to 14pts, varying per batch). Ruled out in turn: JIT
(no-JIT worker also fails), per-job isolation (`queue:work --once` also fails), and a wrong state read
(instrumentation proved the queue and a direct call compute on byte-identical inputs). Root cause:
**SQLite could not handle the `database` queue driver's concurrent access.** Rob chose the root fix:
**moved the app DB to local Postgres 18** — copied all data across (encrypted V2 family intact), re-ran
the family through the identical `queue:work` path, and every plan now reproduces exactly. RESOLVED
(DECISIONS 2026-07-09). NB the mid-investigation family figures reported before the fix were from the
buggy queue path; the canonical figures now stored are correct (sell & buy cheaper £165k 87%, stay-put
29%, let-and-rent 1%).

_2026-07-09 (the let plan given the BTL finance-cost tax relief; #17 rental → £1,800/mo)_ —
Rob asked whether "Let out & rent elsewhere" (#17, 0%) counted rental income. It did (£17,500/yr
reaching the forecast as taxable), but the rent was taxed with no relief for the mortgage interest —
wrong for a BTL since the April-2020 finance-cost restriction. Built the 20%-of-min(interest, rent)
tax reducer on `isLet` properties (engine + `BuyToLetFinanceCostTest`); set #17's rental to £1,800/mo.
Both together move #17 from depleting 2030 → 2035, but it still fails: keeping the £208k mortgage AND
paying rent elsewhere is uncovered by any realistic rent — only selling clears the £208k. Reconsidered
and REJECTED the "let flat over-charges utilities" caveat I'd raised: charging the flat's council
tax/utilities is a fair proxy for the occupier costs they'd pay at the rented home (one set, not
double). Family re-run on the new stamp.

_2026-07-08 later (child-help resolved tax-free; bought-home maintenance built; a self-inflicted mortgage error caught and reverted)_ —
Rob confirmed the child's £300/mo involves no work → gift → tax-free; #23 flipped + renamed. Built the
bought-home 1% maintenance default (the buy plans had modelled zero upkeep on the new freehold because
the flat's upkeep lives in its stripped service charge). Then made a real mistake: I flagged the
stay-put plans' **£118k** mortgage as a "bug" versus the disposal plans' £208k, and changed the base to
£208k / £16,170.96 — **without reading docs/SCENARIO-V2.local.md**, which documents that the £118k is
Rob's deliberate 2026-07-03 design (the £208k BTL cannot be kept while occupied, so "Stay put" models a
~£90k paydown + BTL→residential RIO conversion). **Reverted** the base to £118k / £7,080. Rob's
current-mortgage figures (£208k BTL, £1,347.58/mo) confirm the model's starting point, not a base error.
Also corrected a separate self-error: the service-charge lever moves the base's depletion 2045 → **2043**
(not 2041 as I'd said; verified with/without growth). MC family left stale (maintenance bumped the
engine stamp) pending Rob's go-ahead to re-run for maintenance + #23.

_2026-07-08 later (the two open questions moved; the service-charge lever built + applied)_ —
Rob answered question (1): the child's £300/mo would be paid through the child's own business, which
he read as taxable. Research says the route doesn't decide it — support-not-wages stays a gift (no
income tax for the recipient; the company payment is the CHILD's tax problem, likely a drawing or a
close-company distribution, CTA 2010 s.1064/1069), while genuine work for the business IS taxable
earnings. **Plan #23's row stays `taxable` until Rob confirms which fact pattern applies** (flip it
in the builder or ask here); question (2) (£208k redemption) still open. Then Rob challenged CPI
cost growth with the flat's 12-year service-charge history: analysis showed 2.4%/yr vs ~2.9% CPI
long-run (real DECLINE, the recent £280 rises are post-surge catch-up) but ~CPI+1.5 real over the
last 3 years; his ruling "assume the worst" produced the **propertyCostsRealGrowth lever** (Done
bullet; DECISIONS + DATA-MODEL 2026-07-08), the V2 base set to 1.5% (children inherit; sell/rent
children naturally inert) and the family re-run on runs #321–336. Also verified in passing: the
service-charge line's empty condition auto-classifies safely to while-owning-home, and nearly every
V2 spend line is marked `essential` (incl. Netflix / lottery / charity) — flagged to Rob as worth a
reclassification pass since it inflates the essentials floor behind "Essentials covered", the safety
buffer and the survivor/care analyses.

_2026-07-08 late (resume: the care means-test tail built; V2 family re-run on the new stamp)_ —
Resumed from the handover; suite and git reconciled clean, and the "re-run before reading MC wealth"
carry-over was found already done (16 completed runs on the net-wealth stamp), so the stale note was
dropped. Then the first What's-next-#3 refinement, picked by value (every V2 plan models care): care
years are now charged at the household-borne means-tested amount instead of the gross self-funder fee
(DECISIONS 2026-07-08; the PEA sourced from the DHSC 2026-27 charging circular). The engine stamp
bumped, so the morning's 16 runs went stale for care figures; the family was re-queued headlessly
(runs #289–304, the same dispatch Compare's "Re-run all N" makes) and completed on
`phase-3-care-means-test`. Panel/builder/assistant copy updated to the household-borne reading; the
methodology doc rewritten for the means test and its v1 flags; the assistant doc index rebuilt (44
chunks). **The two input questions for Rob below remain open** (the taxable child-help row on the buy
plan; the £208k redemption figure).

_2026-07-08 evening (Rob caught the gross-wealth headline; all wealth went net; every what-if renamed)_ —
Compare's "most money at the end" answer credited the LTM roll-up combination with its home **gross**
(£595,694.63); Rob challenged it — a rolled-up lifetime mortgage consumes the equity. Root cause: the
2026-07-06 build kept `totalWealth` gross and surfaced `netWealth()` beside it, but nothing displayed
`netWealth()` and no test pinned the **terminal headline** — every "incl. home" figure (Compare, results,
PDF, CSV, MC percentiles, assistant) read gross. Fix (Rob: "all figures should be net; gross is
irrelevant… she can't eat the building"): the net-of-mortgage total is now derived in `YearResult` — one
definition, every surface inherits (Done bullet; DECISIONS + DATA-MODEL 2026-07-08). Corrected V2
ranking: best total "YCC works to 72 + full SP £241/wk, no disability benefit" £479,604.55; best
spendable "Sell (repay £208k) & buy cheaper £165k" £172,613.70; the old "winner" is really £277,187.58
(~£121k real equity survives the roll-up — house growth partly races the 6.5%). Stay-put totals dropped
~£75k (the static £118k repayment balance, real-deflated, now visibly counts — see the new v1 limit).
Then **all 15 what-if children renamed** to state their modelled changes (worst: "Let to Rent + Retire 5
years later" contained no letting — it models retire-at-72 + full SP + disability benefit off). **Two
input questions for Rob:** (1) the buy plan's child-help income is stored **taxable** while the stay-put
ones are tax-free — a gift isn't taxable income, so that plan is likely under-credited; (2) every
disposal plan raises the mortgage owed £118k → £208k at sale — confirm £208k is the true redemption
figure. Scratch scripts (dump/rename/re-run) in the session scratchpad, not the repo.

_Older sessions (2026-07-08 afternoon and earlier) are folded to keep this log to the live window. They
cover: the assistant-504 fix plus the composed LTM/part-time what-if; the freshness-CI and a11y
housekeeping; decision-support Phases 0 to 6; the spending "smile"; equity release; IHT wired into the
forecast; the local-model assistant (all three phases); the Phase A/B/C/D builds; the five silent-drop
completeness fixes; and the personal-data scrub. Each has its Done bullet above, a dated DECISIONS entry,
and the full record in git log._
